Backup of the Our Team page (post 20) body content as it existed before
the team-pages work replaced it on 2026-07-29.

  bytes: 36550
  md5:   cc259ecc90f19e0bb74537987a24b842

Restore with (reads the file directly, so the restore is byte-exact):

  wp post update 20 docs/backups/page-20-our-team-original-content.txt

Do NOT use --post_content="$(cat ...)" — command substitution strips the
trailing newline and the restored content will not match the md5 above.

The replacement layout is generated by bin/seed-team-content.php, which
refuses to overwrite an edited page unless run with the 'force' argument.

## Team member body layout (Divi Theme Builder)

`body-layout-109656-current.txt` is the known-good state of the member page's
body layout: header section (no background, 0 padding), articles section
(#dad4e3 -> #ffffff vertical gradient, 89px top/bottom padding) and the CTA
section.

This lives in the DATABASE, not in this repository, so it is not covered by any
commit. It has already been lost once: a save through the Divi UI re-persisted
an earlier state and silently discarded a wp-cli edit. Re-apply with:

    wp post update 109656 docs/backups/body-layout-109656-current.txt

Divi caches per-post CSS, so any scripted edit must be followed by:

    rm -rf wp-content/et-cache/*
    wp eval 'ET_Core_PageResource::remove_static_resources("all","all");'

Without that the layout changes but the page does not.
